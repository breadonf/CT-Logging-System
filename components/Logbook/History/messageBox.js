import React from "react";
import {
  Card,
  CardContent,
  CardActionArea,
  Typography,
  Grid,
} from "@mui/material";

const MessageBox = ({ user, category, HTMLmessage }) => {
  return (
    <Grid item>
      <Card></Card>
    </Grid>
  );
};

export default MessageBox;
